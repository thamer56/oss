{
    "version": 2,
    "buildCommand": "npm run vercel-build",
    "outputDirectory": "dist/my-angular-app",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}